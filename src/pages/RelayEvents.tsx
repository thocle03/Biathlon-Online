import { EventsByType } from './EventsByType';

export const RelayEvents = () => {
    return (
        <EventsByType
            type="relay"
            title="Événements Relais"
            description="Courses par équipes avec passage de relais"
        />
    );
};

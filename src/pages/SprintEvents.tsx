import { EventsByType } from './EventsByType';

export const SprintEvents = () => {
    return (
        <EventsByType
            type="sprint"
            title="Événements Sprint"
            description="Courses en duel avec 2 tirs (couché et debout)"
        />
    );
};
